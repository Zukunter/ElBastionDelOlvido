# Bastión del Olvido : _Neovim_
## Installation 
```bash
[ -d ~/.config/nvim ] && mv ~/.config/nvim ~/.config/nvim.bank || mkdir -p ~/.config/nvim && git clone https://github.com/Zukunter/Neovim.git ~/.config/nvim
```

