plugin = /usr/lib/hyprland/plugins/hyprexpo.so

plugin {
    hyprexpo {
        columns = 3
        gap_size = 5
        bg_col = rgb(092425)
        workspace_method = center current # [center/first] [stack/regexp/id/name]
        gesture_distance = 300
        skip_empty = true
    }
}
